/**
 * EAS replaces this committed fallback during its post-install hook.
 * Direct local and simulator builds intentionally remain unqualified.
 */
export const BUILD_SOURCE_REVISION: string | null = null;
